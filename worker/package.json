{
  "name": "wellness-center-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute wellness-center-db --local --file=schema.sql",
    "db:migrate": "wrangler d1 execute wellness-center-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.81",
    "ai": "^6.0.197",
    "zod": "^4.4.3"
  }
}
