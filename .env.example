# Dev API key - must match the API_KEY secret set on the Cloudflare Worker
VITE_API_KEY=dev-secret-change-me

# Leave VITE_API_URL unset in dev - vite proxy forwards /api to localhost:8787
# VITE_API_URL=https://wellness-center-api.<account>.workers.dev
